cmake_minimum_required(VERSION 3.20)
project(LMImpactExample)
if(SKIP_SEMLA_SRC_TESTS)
    message(STATUS "Skipping test builds since SKIP_SEMLA_SRC_TESTS is set" )
else()
    include(CTest)
endif()
include(${CMAKE_CURRENT_LIST_DIR}/openssl/CMakeLists.txt)
include(${CMAKE_CURRENT_LIST_DIR}/SEMLA/CMakeLists.txt)
